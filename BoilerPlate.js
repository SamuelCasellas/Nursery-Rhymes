import React from "react";
import { View, StyleSheet } from "react-native";
import { Text } from "react-native-elements"

const ScreenName = () => {
  return (
    <View>
      <Text h1>ScreenName</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ScreenName;
